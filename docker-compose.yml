version: "3.8"

services:

  backend:
    build: ./backend
    container_name: prescripto_backend

    ports:
      - "4000:4000"

  frontend:
    build: ./frontend
    container_name: prescripto_frontend

    ports:
      - "5173:5173"

  admin:
    build: ./admin
    container_name: prescripto_admin

    ports:
      - "5174:5174"