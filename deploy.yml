- hosts: localhost

  connection: local

  tasks:

    - name: Stop Existing Containers

      command: docker compose down

      args:
        chdir: /mnt/c/ProgramData/Jenkins/.jenkins/workspace/prescripto-project

      ignore_errors: yes

    - name: Build and Start Containers

      command: docker compose up --build -d

      args:
        chdir: /mnt/c/ProgramData/Jenkins/.jenkins/workspace/prescripto-project